services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      DJANGO_SETTINGS_MODULE: runclub.settings.dev
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: runclub
      POSTGRES_USER: runclub
      POSTGRES_PASSWORD: runclub
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U runclub"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
